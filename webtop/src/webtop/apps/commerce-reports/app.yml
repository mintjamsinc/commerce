identifier: a1c9e2f4-3b7d-4e18-9a52-6c0f8b1d4e39
title: Commerce Reports
category: commerce
icon: assets/icons/icon.svg
isAdminOnly: true
# The occurrence-date sales summary console: daily new-order / cancellation /
# refund counts and amounts (each event on its own date) over an operator-picked
# date range, with CSV export (reports.groovy?type=occurrence). Read-only — one
# app, one concern (split out of the retired Commerce Operations console).
actions:
  open:
    icon: open
    label: Open
minimumWidth: 760
minimumHeight: 560
customWindowControls: true
singleton: true
